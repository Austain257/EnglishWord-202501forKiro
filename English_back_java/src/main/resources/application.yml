server:
  port: 8080

spring:
  application:
    name: EnglishAndWord
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 30MB
      resolve-lazily: true
      enabled: true
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/english_for_kiro?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: Austain
      password: 123456


#  datasource:
#      driver-class-name: com.mysql.cj.jdbc.Driver
#      url: jdbc:mysql://8.148.209.41:3306/english?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
#      username: austain
#      password: 123456

#  datasource:
#      driver-class-name: com.mysql.cj.jdbc.Driver
#      url: jdbc:mysql://localhost:3306/english?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
#      username: root
#      password: 1234


  ai:
    openai: # openai模型配置
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      api-key: ${OPENAI_API_KEY} # API key配置在电脑环境变量中，使用此符号引用（如何配置看Function Calling笔记）
      chat:
        options:
          model: deepseek-v3.1
          temperature: 0.7

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

# 阿里云OSS配置
aliyun:
  oss:
    endpoint: oss-cn-beijing.aliyuncs.com  # 根据你的OSS区域修改
    access-key-id: ${ALIYUN_OSS_ACCESS_KEY_ID}  # 建议使用环境变量
    access-key-secret: ${ALIYUN_OSS_ACCESS_KEY_SECRET}  # 建议使用环境变量
    bucket-name: austain-java-ai-web  # 你的bucket名称
    base-url: https://austain-java-ai-web.oss-cn-beijing.aliyuncs.com  # 访问域名
    upload-dir: english_avatar_20260114/  # 头像上传目录
